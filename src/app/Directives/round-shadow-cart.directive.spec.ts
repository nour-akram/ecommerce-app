import { RoundShadowCartDirective } from './round-shadow-cart.directive';

describe('RoundShadowCartDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundShadowCartDirective();
    expect(directive).toBeTruthy();
  });
});
