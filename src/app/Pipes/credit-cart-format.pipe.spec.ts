import { CreditCartFormatPipe } from './credit-cart-format.pipe';

describe('CreditCartFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCartFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
